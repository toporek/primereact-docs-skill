# ToggleButton Theming

Theming documentation for ToggleButton component

## Styled

### ToggleButton CSS Classes

List of class names used in the styled mode.

> **API/props table for `ToggleButton` (`style`) is generated from upstream TypeScript types and is not yet mirrored — see the installed package types or the upstream docs. (TODO: mirror in v2.)

### ToggleButtonGroup CSS Classes

List of class names used in the styled mode.

> **API/props table for `ToggleButtonGroup` (`style`) is generated from upstream TypeScript types and is not yet mirrored — see the installed package types or the upstream docs. (TODO: mirror in v2.)

### Design Tokens

List of design tokens.

> **API/props table for `ToggleButton` (`token`) is generated from upstream TypeScript types and is not yet mirrored — see the installed package types or the upstream docs. (TODO: mirror in v2.)

## Unstyled

Theming is implemented with the pass through properties in unstyled mode.
