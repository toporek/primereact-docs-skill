# useIconField

Hook that positions an icon inside an input field boundary.

## Usage

```tsx
import { useIconField } from '@primereact/headless/iconfield';
```

```tsx
const instance = useIconField();
```

## API

### useIconField

> **`useIconField` API table (`api`)** — TypeScript-derived; not inlined here. See the live table at https://primereact.dev/docs/headless/components/iconfield or the installed `@primereact/types`.
