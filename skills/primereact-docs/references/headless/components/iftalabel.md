# useIftaLabel

Hook that positions a top-aligned label inside the input field boundary.

## Usage

```tsx
import { useIftaLabel } from '@primereact/headless/iftalabel';
```

```tsx
const instance = useIftaLabel();
```

## API

### useIftaLabel

> **`useIftaLabel` API table (`api`)** — TypeScript-derived; not inlined here. See the live table at https://primereact.dev/docs/headless/components/iftalabel or the installed `@primereact/types`.
