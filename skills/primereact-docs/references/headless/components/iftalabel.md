# useIftaLabel

Hook that positions a top-aligned label inside the input field boundary.

## Usage

```tsx
import { useIftaLabel } from '@primereact/headless/iftalabel';
```

```tsx
const instance = useIftaLabel();
```

## API

### useIftaLabel

> **API/props table for `useIftaLabel` (`api`) is generated from upstream TypeScript types and is not yet mirrored — see the installed package types or the upstream docs. (TODO: mirror in v2.)
