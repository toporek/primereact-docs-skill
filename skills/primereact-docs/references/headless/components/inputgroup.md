# useInputGroup

Hook that composes input fields with adjacent addon elements.

## Usage

```tsx
import { useInputGroup } from '@primereact/headless/inputgroup';
```

```tsx
const instance = useInputGroup();
```

## API

### useInputGroup

> **`useInputGroup` API table (`api`)** — TypeScript-derived; not inlined here. See the live table at https://primereact.dev/docs/headless/components/inputgroup or the installed `@primereact/types`.
