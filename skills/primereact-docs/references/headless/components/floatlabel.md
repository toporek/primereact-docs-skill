# useFloatLabel

Hook that coordinates an input and a floating label with focus and value state.

## Usage

```tsx
import { useFloatLabel } from '@primereact/headless/floatlabel';
```

```tsx
const instance = useFloatLabel();
```

## API

### useFloatLabel

> **`useFloatLabel` API table (`api`)** — TypeScript-derived; not inlined here. See the live table at https://primereact.dev/docs/headless/components/floatlabel or the installed `@primereact/types`.
