# useLabel

Hook that associates a label with an input control via id and htmlFor.

## Usage

```tsx
import { useLabel } from '@primereact/headless/label';
```

```tsx
const instance = useLabel();
```

## API

### useLabel

> **`useLabel` API table (`api`)** — TypeScript-derived; not inlined here. See the live table at https://primereact.dev/docs/headless/components/label or the installed `@primereact/types`.
