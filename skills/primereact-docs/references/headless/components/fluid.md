# useFluid

Hook that propagates fluid sizing context to descendant form components.

## Usage

```tsx
import { useFluid } from '@primereact/headless/fluid';
```

```tsx
const instance = useFluid();
```

## API

### useFluid

> **`useFluid` API table (`api`)** — TypeScript-derived; not inlined here. See the live table at https://primereact.dev/docs/headless/components/fluid or the installed `@primereact/types`.
